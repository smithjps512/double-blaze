# Where this team's documents came from

Transcribed from Google Drive, folder
`7th period / 7th period app / Oragutangs` (the folder name is misspelled; the
team calls itself Team Orangutan inside the document).

- `app product plan.pdf`: the plan. `app product plan2.pdf` is a byte-identical
  re-upload and was skipped.
- `My User Stories.pdf`: four stories in one document. Quiz, video, AI
  assistant and placement test.
- `My User Stories2.pdf`: secret age groups.

**Removed in transcription:** both plans opened with the three authors' full
names. This repository is public, so they are not reproduced.

Also changed: the feature table was flattened into a list, spelling corrected,
template scaffolding removed. Nothing was added: the secret age groups story has
an empty Given/When/Then and it stays empty, so the prototype reports it.

One thing for the team: **the plan never names the app**, so the prototype is
titled after the team. This is the most complete set of stories in the period
and it deserves a name.
